name: blogAPI CI/CD

on:
  push:
    branches:
      - "main"
      - "master"

jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
      - name: Push repo
        uses: actions/checkout@v3
      - name: Print hello
        run: echo "Hello world"
